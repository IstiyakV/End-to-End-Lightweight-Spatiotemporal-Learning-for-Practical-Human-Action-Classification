<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="12dp"
    android:layout_marginHorizontal="16dp"
    android:layout_marginVertical="8dp"
    android:background="@drawable/bg_chat_bubble_ai">

    <!-- Card Title / Prediction -->
    <TextView
        android:id="@+id/cardResultText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Predicted Action: Billiards"
        android:textColor="#a855f7"
        android:textSize="16sp"
        android:textStyle="bold"
        android:textAlignment="center"
        android:layout_marginBottom="4dp"/>

    <TextView
        android:id="@+id/cardConfidenceText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Confidence: 86.3%"
        android:textColor="#FFFFFF"
        android:textSize="13sp"
        android:textAlignment="center"
        android:layout_marginBottom="8dp"/>

    <!-- Confidence Progress Bar -->
    <ProgressBar
        android:id="@+id/cardConfidenceProgress"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="6dp"
        android:progressDrawable="@drawable/progress_bar_neon"
        android:max="100"
        android:progress="86"
        android:layout_marginBottom="12dp"/>

    <!-- Grad-CAM Video Preview Image -->
    <ImageView
        android:id="@+id/cardImageView"
        android:layout_width="match_parent"
        android:layout_height="220dp"
        android:scaleType="fitCenter"
        android:background="#1a103c"
        android:adjustViewBounds="true"
        android:layout_marginBottom="4dp"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Grad-CAM Spatiotemporal Visualisation Heatmap"
        android:textColor="#8b82a0"
        android:textSize="10sp"
        android:textStyle="italic"
        android:textAlignment="center"/>

</LinearLayout>
